package model;

import java.time.LocalDateTime;

public class CardPayment implements PaymentStrategy{
	public void recordPayment(Boolean done,LocalDateTime time) {
		//only to enter a different name in database
	}
}
